{% layout none %}
<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Gift card for {{ shop.name }}</title>
    <link href="https://fonts.googleapis.com/css2?family=Lora:wght@600&family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
      body { font-family: Poppins, sans-serif; background: #F6F4F9; color: #2E2A39; text-align: center; padding: 4rem 1.5rem; margin: 0; }
      .card { background: #fff; border-radius: 16px; max-width: 420px; margin: 0 auto; padding: 2.5rem; box-shadow: 0 12px 32px rgba(46,42,57,.1); }
      h1 { font-family: Lora, serif; }
      .amount { font-size: 2.5rem; font-weight: 600; color: #6D388B; }
      .code { background: #F6F4F9; border-radius: 10px; padding: 1rem; font-size: 1.25rem; letter-spacing: .12em; margin: 1.5rem 0; }
      a.btn { display: inline-block; background: #6D388B; color: #fff; text-decoration: none; padding: .9rem 2rem; border-radius: 8px; font-weight: 600; }
    </style>
  </head>
  <body>
    <div class="card">
      <div style="font-size:3rem;">🎈</div>
      <h1>{{ shop.name }} gift card</h1>
      <p class="amount">{{ gift_card.initial_value | money }}</p>
      {% if gift_card.enabled == false or gift_card.expired %}
        <p>This gift card is no longer valid.</p>
      {% endif %}
      <div class="code">{{ gift_card.code | format_code }}</div>
      <a class="btn" href="{{ shop.url }}">Start shopping</a>
    </div>
  </body>
</html>
